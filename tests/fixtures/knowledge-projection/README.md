# Knowledge projection fixture

`knowledge-projection-check.py` expands this bounded fixture description into
100 approved claims, three approved events, and one case-scoped story arc. The
fixture is generated in memory so it contains no private case evidence.
