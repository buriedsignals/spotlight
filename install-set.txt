# Spotlight install set — the directories a frontier runtime (Claude Code, Codex,
# Cursor, Gemini, Goose) needs to run the skills. Root files (AGENTS.md, LICENSE,
# DISCLAIMER.md) are always included by git cone-mode sparse checkout.
#
#   git sparse-checkout set $(grep -v '^#' install-set.txt)
#
# Not listed: cases/ (your investigations live outside the checkout), harness/
# (non-frontier local-model runtime), docs/ evals/ tests/ (development), and
# plugins/ together with the .claude-plugin/ and .agents/ marketplace manifests
# that point at it — those serve the plugin-marketplace route, which clones the
# full repository itself (README, "Plugin install").
skills
agents
scripts
schemas
integrations
monitoring
third_party
upstreams
